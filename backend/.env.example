# Server Configuration
PORT=3000
HOST=0.0.0.0
FRONTEND_URL=http://localhost:5173

# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/millitime

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production

# CoinGecko API (optional - for API key if you upgrade)
COINGECKO_API_KEY=

# AI Trading Enhancement (Optional - for AI-powered fundamental analysis)
# Get your keys from: https://platform.openai.com/ or https://console.anthropic.com/
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Gemini API Keys (https://aistudio.google.com/apikey)
# Option 1: Multiple keys for rotation (recommended for high volume)
# Comma-separated list of API keys. Each key gets 250 requests/day.
# Example: 9 keys × 250 = 2,250 requests/day
GEMINI_API_KEYS=key1,key2,key3,key4,key5,key6,key7,key8,key9

# Option 2: Single key (for low volume)
#GEMINI_API_KEY=your_single_key_here

# Preferred AI provider: auto, openai, anthropic, or gemini (default: auto)
AI_PROVIDER=auto
# Enable AI-enhanced signals (default: true)
ENABLE_AI_ANALYSIS=true

# Gemini API Rate Limits (Free tier defaults per key)
# Requests per minute per key (default: 10)
GEMINI_RPM_LIMIT=10
# Tokens per minute per key (default: 250000)
GEMINI_TPM_LIMIT=250000
# Requests per day per key (default: 250)
GEMINI_RPD_LIMIT=250
